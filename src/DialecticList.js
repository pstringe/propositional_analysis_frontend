import { Box } from '@mui/material';

const cardStyles = {
    width: '100%',
    height: '50px',
};

const DialecticList = () => {
    return ( <Box styles={cardStyles}>
        <h1>Dialectic</h1>

    </Box> );
}
 
export default DialecticList;