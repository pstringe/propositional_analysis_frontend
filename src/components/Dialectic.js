import { Card, Box } from '@mui/material';

const cardStyles = {
    width: '100%',
    height: '50px',
};

const Dialectic = () => {
    return ( 
        <Box style={cardStyles}>
            <Card />
        </Box>
    );
}

export default Dialectic;